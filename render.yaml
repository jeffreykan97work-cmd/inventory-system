services:
  - type: web
    name: dma-inventory
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: DB_PATH
        value: /data/inventory.db
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
